# Migration preview

`.skillsrc` is a preview-only grouped translation of the current manifest. It preserves all 26 supplied skill entries despite the earlier 25-skill description.

Do not run it against the live target, lockfile, or cache, and do not mutate live agent state from this fixture.
